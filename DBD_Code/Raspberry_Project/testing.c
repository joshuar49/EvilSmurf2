#include <stdio.h>

int main()
{
	// I will make a function that takes in a list of strings 
	// and appends them ot a an array of 2d 
	// then prints them out
	int counter = 0;
	char* str;
	printf("please enter a string: ");
	while (scanf("%s" != '\n')){
		printf("value: \n");
		scanf("%s", &arr1);
	}
	printf("Here is the value entered: %s", arr1);
	return 0;
}
